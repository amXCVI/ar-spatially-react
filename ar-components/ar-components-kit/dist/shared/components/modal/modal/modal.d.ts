import { ReactNode } from 'react';

interface ModalProps {
    children: ReactNode;
}
declare const Modal: ({ children }: ModalProps) => import("react/jsx-runtime").JSX.Element;
export default Modal;
